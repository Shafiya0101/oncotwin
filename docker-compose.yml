services:
  api:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data

  web:
    image: python:3.11-slim
    working_dir: /web
    volumes:
      - .:/web
    command: python -m http.server 8080
    ports:
      - "8080:8080"
    depends_on:
      - api
